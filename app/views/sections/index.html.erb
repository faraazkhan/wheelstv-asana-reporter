<h1>Listing sections</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sections.each do |section| %>
  <tr>
    <td><%= section.name %></td>
    <td><%= link_to 'Edit', edit_project_section_path(@project, section) %></td>
    <td><%= link_to 'Destroy', project_section_path(@project, section), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Section', new_project_section_path(@project) %>
